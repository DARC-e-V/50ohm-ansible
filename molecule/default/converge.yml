---
- name: Converge
  hosts: all
  become: true
  vars_files:
    - ../../vars.yml
    - ../../users.yml
  roles:
    - role: base
    - role: install-sources
    - role: build-50ohm
    - role: refresh-50ohm
